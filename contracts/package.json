{
  "name": "greenledger-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "GreenLedger achievement badge contracts (ERC-1155, Ethereum Sepolia testnet)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
