fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
numpy>=1.26.0
pandas>=2.2.0
xgboost>=2.0.0
scikit-learn>=1.4.0
pytest>=8.0.0
httpx>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
# Agent collector contract is covered by the backend suite (attention signals).
psutil>=5.9.0
# Temporal LSTM intervals (Phase 2, optional): install the CPU wheel to avoid
# the multi-GB CUDA bundle — pip install torch --index-url
# https://download.pytorch.org/whl/cpu — then: pip install -r requirements.txt
torch>=2.0.0
